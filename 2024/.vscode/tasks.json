{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "zig build",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}/${config:zig_project_dir}/"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}